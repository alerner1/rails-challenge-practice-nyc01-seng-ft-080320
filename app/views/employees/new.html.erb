<%= form_for @employee do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.collection_select :company_id, Company.all, :id, :name %><br />
  <%= f.submit %>
<% end %>